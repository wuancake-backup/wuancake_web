@using System.Collections;
    @{
        int oneprice = 5;
        ArrayList onelist = new ArrayList();
        for(int i =0;i<oneprice;i++)
        {
            onelist.Add(1);
        }
        int twoprice = 10;
        ArrayList twolist = new ArrayList();
        for (int i = 0; i < twoprice; i++)
        {
            onelist.Add(2);
        }
        int threeprice = 15;
        ArrayList threelist = new ArrayList();
        for (int i = 0; i < twoprice; i++)
        {
            onelist.Add(3);
        }
        int people = 2000;
        int nopoepli = people - oneprice - twoprice - threeprice;
        ArrayList nope = new ArrayList();
        for (int i = 0; i < nopoepli; i++)
        {
            onelist.Add(0);
        }
        int len = onelist.Count;
        Random rand = new Random();
        int obj = Convert.ToInt32(onelist[rand.Next(0, len - 1)]);
        string prize = "";
        int result=0;
        if(obj==0)
        {
            prize = "谢谢参与";
            int[] pos=new int[]{1,3,5,7};
            Random o = new Random();
            result = pos[o.Next(pos.Length)];
            onelist.Remove(0);
        }
        else if(obj==1)
        {
            prize = "一等奖";
            result = 0;
            onelist.Remove(1);
        }
        else if(obj==2)
        {
            prize = "二等级";
            result = 4;
            onelist.Remove(2);
        }
        else if(obj==3)
        {
            prize = "三等奖";
            result = 8;
            onelist.Remove(3);
        }
        //string stu = "{\"result\":\""+ result +"\" ,\"prize\":\"" + prize + "\"}";  
        @result; return;
    
    }