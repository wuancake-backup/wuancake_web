rgba用作透明比opacity好不少
    他们相同之处就是背景色完全是一样的，但区别就是是opacity后代元素会随着一起具有透明性，所以我们Opacity中的字随着透明值下降越来越看不清楚，而RGBA不具有这样的问题
    项目里如果这首歌没图片就会只显示一个背景色和歌名，用opacity会导致歌名也透明了，非常的咸鱼。

之前看到个css的缩进
    如果是后代的话像编程语言一样加一个tab，非常清晰，以后我就用这种方法了。